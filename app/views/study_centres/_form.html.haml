= form_for @study_centre do |f|
  - if @study_centre.errors.any?
    #error_explanation
      %h2= "#{pluralize(@study_centre.errors.count, "error")} prohibited this study_centre from being saved:"
      %ul
        - @study_centre.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :references
    = f.text_field :references
  .field
    = f.label :references
    = f.text_field :references
  .actions
    = f.submit 'Save'
