- breadcrumb :participant_event_form, @participant_event_form
%p
%p
  %strong
    =@participant_event_form.participant.participant_identifier
    =@participant_event_form

  = render 'form'

-#= link_to 'Show', @participant_event_form
-#\|
  -#= back_to_participant(@participant_event_form.participant)