%p#notice= notice
%h1 Listing Study Users
%table
  %thead
    %tr
      %th Study
      %th User
      %th{:colspan => "3"}
  %tbody
    - @study_users.each do |study_user|
      %tr
        %td= study_user.study.name
        %td= study_user.user.name
        %td= link_to 'Show', [study_user.study, study_user]
        %td= link_to 'Edit', edit__study_user_path(study_user.study, study_user)
        %td= link_to 'Destroy', [study_user.study, study_user], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Study user', new__study_user_path
