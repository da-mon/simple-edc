<%= form_for(@study_user) do |f| %>
  <% if @study_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_user.errors.count, "error") %> prohibited this study_user from being saved:</h2>

      <ul>
      <% @study_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
