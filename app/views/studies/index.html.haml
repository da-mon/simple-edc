%p
%p %p %strong Listing Studies
%table.table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @studies.each do |study|
      %tr
        %td= link_to study, edit_study_path(study)
        = link_to_delete(study)
%br/
= link_to add, new_study_path
