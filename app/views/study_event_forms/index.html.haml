%p#notice= notice
%h1 Listing Study Event Forms
%table
  %thead
    %tr
      %th Study event
      %th Study form
      %th{:colspan => "3"}
  %tbody
    - @study_event_forms.each do |study_event_form|
      %tr
        %td= study_event_form.study_event
        %td= study_event_form.study_form
        %td= link_to 'Show', study_event_form
        %td= link_to 'Edit', edit_study_event_form_path(study_event_form)
        %td= link_to 'Destroy', study_event_form, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Study event form', new_study_event_form_path
